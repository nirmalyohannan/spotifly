import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1DB954);
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF282828); // For cards
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3B3B3);
  static const Color error = Color(0xFFE91E63); // Just a standard error color
  
  // Bottom nav bar
  static const Color bottomNavBackground = Color(0xFF121212); // Or slightly lighter?
  
  // Gradients
  static const Color gradientStart = Color(0xFF404040); // Example for gradient backgrounds
  static const Color gradientEnd = Color(0xFF121212);
}
